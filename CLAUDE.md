# Project rules

1. Shared form validation must live in a single module and be reused by both tests and the browser UI.
2. Every form change must include at least one edge-case test, such as malformed input or unsupported values.
3. Accessibility is part of delivery: error text and status updates must be associated with form controls through ARIA or labels.
